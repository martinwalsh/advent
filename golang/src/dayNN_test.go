package main

import "testing"

func TestDayNNPart1(t *testing.T) {
    got := dayNN_part2()
    if !got {
        t.Errorf("got '%t' want '%t'", got, false)
    }
}

func TestDayNNPart2(t *testing.T) {
    got := dayNN_part2()
    if !got {
        t.Errorf("got '%t' want '%t'", got, false)
    }
}
